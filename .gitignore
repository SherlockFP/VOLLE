data/profiles.json
data/profiles.json.tmp
data/accounts.db
.opencode/
.serena/
graphify-out/
.claude/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
# Local runtime analytics can contain pseudonymous profile/session identifiers.
data/product-analytics.json

# Local development QA logs and temporary probes.
.qa/

# Local rollback checkpoints; keep large working snapshots out of commits.
checkpoints/
