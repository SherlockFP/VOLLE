# 2026-07-31 — Fable 5 orkestrasyon oturumu

Bağlam: omp (ohmypi) oturumu PC kapanınca yarım kaldı; Claude Code (Fable 5) devraldı.
Model dağıtımı: Fable 5 plan/kontrol, Opus 5 derin debug/kompleks feature, Sonnet 5 sınırlı UI işleri, Haiku commit.

## Faz 1 — omp'nin yarım işleri (commit `1741825`, pushlu)
- omp'nin uncommitted diff'i devralındı (quick-play reroute, skybox revert, test güncellemeleri) + `window.__app` TEMP DEBUG satırı silindi.
- **BallStall (Opus):** kök neden `js/main.js` RAF loop `!isHost` guard'ı + bg loop `!connected` guard'ı → `isHost && !connected` durumunda sıfır `game.update()`. `hostGame()` `initPeer()` await'inden ÖNCE `isHost=true` set ediyordu; tek başarısız Create Lobby sekmedeki tüm sonraki solo maçları donduruyordu. Ball/bot mantığı hiç bozuk değildi (suspect commit'ler temiz, `5a29b05`'e blame). Fix: RAF guard `!isHost || !connected` + hostGame catch'te isHost reset.
- **HostJoin (Opus):** (1) `btn-start-game` maç başlarken lobiyi unregister ediyordu → late-join UI'dan ulaşılamazdı; (2) TTL 45s < Chrome gizli-sekme 60s timer clamp; (3) rate limit IP-bazlı, lokal sekmeler paylaşıyor, sessiz 429; (4) `peer-unavailable` dinlenmiyor → ölü kod Join'i asıyor.
- Doğrulama: full suite 1222/1222 bizzat koşuldu; canlı smoke: gizli sekmede RAF ölü olduğu için `Game.prototype.startSolo` patch'le instance yakala + `setInterval(g.update(1/60))` pump → ROUND_END→PLAYING→ROUND_END döngüsü, top hareketli, vuruş oluyor.

## Faz 2 — geliştirme dalgası (2026 mobil/io retention standartları)
- **Wave A1 (Opus, post-match):** ödül akışı — detay [[../STATUS]] + `tests/post-match-rewards.test.mjs`.
- **Wave A2 (Sonnet, menü):** `#menu-retention-strip` + micro-interactions; streak badge zaten V4 Wave 4'te varmış, katlandı.
- **Wave B (Sonnet, FTUE):** oturum kapanırken çalışıyordu — sonucu STATUS'a işlenecek.
- Dosya sahipliği ayrımıyla sıfır çakışma (AGENTS.md kural 5). polish.css iki agent'ın ayrı append bloklarını aldı, selector çakışması yok.

## Ortam tuzakları (tekrar yaşama)
- Sandbox browser pane görünmüyorsa `document.hidden=true` → RAF atmaz, screenshot alınamaz; smoke için manuel update pump veya DOM/computed-style kontrolü.
- Pane `prefers-reduced-motion: reduce` zorluyor — animasyon doğrulaması computed `transition` üzerinden.
- `node --test` glob: `node --test "tests/*.test.mjs" "tests/*.test.cjs"` (~30s, şu an 1233).
- omp session transkriptleri: `~/.omp/agent/sessions/<cwd-slug>/*.jsonl`.

## Kapanış eki
- Wave E (Opus): bıçak silüetleri CSS custom-property tabanına geçti (7 farklı), ball shape 3D inspect `ballShapeParts()` clone ile, CS inventory grid, HUD canlandırma (`:has()` Chromium'da style engine'de eşleşmedi → explicit `hud-critical` class), patch notes v0.11. Geçerken 3 kart-buton kırpma bug'ı fixlendi. 1272/1272.
- graphify --update: 253 kod AST + 49 doc semantic (4 Sonnet chunk agent; ilk 2 deneme JSON'u reply'a basıp 64k output limitine takıldı — ders: extraction subagent'ına "Write tool ile yaz, reply'a basma" + node/edge tavanı ŞART). 47 asset görseli bilerek atlandı (graph değeri yok). Sonuç: 6320 node / 12796 edge / 436 topluluk, top-20 topluluk elle etiketli.
- Kapanış commitleri haiku ile. Bkz [[../STATUS]].
