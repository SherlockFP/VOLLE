{
  "name": "warrball",
  "version": "1.0.0",
  "private": true,
  "description": "Warrball competitive first-person ball combat game",
  "scripts": {
    "prestart": "node scripts/build.mjs --if-stale",
    "start": "node server.js",
    "test": "node --test",
    "check": "node scripts/check-js.js",
    "build": "node scripts/build.mjs",
    "balance": "node scripts/balance-report.js",
    "graft:build": "npx --yes @nanonets/graft@0.9.1 build .",
    "graft:check": "npx --yes @nanonets/graft@0.9.1 check .",
    "graft:map": "npx --yes @nanonets/graft@0.9.1 map ."
  },
  "engines": {
    "node": ">=22.13"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
